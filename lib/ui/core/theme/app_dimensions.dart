abstract class AppDimensions {
  // Fatores de tamanho
  static const double cardWidthFactor = 0.50;
  static const double cardHeightFactor = 0.30;
  static const double avatarRadiusFactor = 0.15;
  static const double leadingWidth = 0.18;

  // Tamanhos fixos
  static const double iconSize = 42.0;

  // Duração
  static const int defaultAnimationDurationInMs = 400;
}
